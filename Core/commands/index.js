const { knownCommands } = require('./_commands');
const { knownModCommands } = require('./_modCommands');
const { knownNoncommands } = require('./_noncommands');

module.exports = {
  knownCommands,
  knownModCommands,
  knownNoncommands
};
